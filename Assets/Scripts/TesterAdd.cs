using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TesterAdd : MonoBehaviour
{
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Space))
        {
            //Tester tester = FindObjectOfType(Tester);
        }   
    }
}
